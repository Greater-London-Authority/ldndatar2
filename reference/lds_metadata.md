# Fetch Datastore metadata

Fetch all available meta data

## Usage

``` r
lds_metadata(type = "resources")
```

## Arguments

- type:

  A character, the metadata to return: "resources", "datasets", "teams"
  or "topics"

## Value

A dataframe.
